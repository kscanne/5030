Write your code for the code review exercise in this directory.

To run the application, run the `main()` function in `WordToLowerCase` class.

Test class was created using JUnit. `WordtoLowerCaseTest` class gets examples from `tests.tsv` file (in tests -> resources).
To run the test class go to the Run/Debug Configuration -> Edit configuration -> Add New Configuration -> Choose JUnit 
and specify the test class. 
